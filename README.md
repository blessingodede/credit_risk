This project implements a machine learning pipeline for kidney disease detection.
It includes:
- Data preprocessing
- Model training (Decision Tree, Random Forest, Logistic Regression, XGBoost)
- Model evaluation with ROC/AUC
- CI/CD pipeline using GitHub Actions

## How to Run

1. Install dependencies:
```bash
pip install -r requirements.txt
# Trigger workflow
